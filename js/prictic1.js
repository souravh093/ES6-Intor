// Write an arrow function that will take 3 parameters, will multiply the parameters 
// and will return the result.
const goOn = (num1, num2, num3) => num1 * num2 * num3;
const haveOne = goOn(5, 9, 10);
console.log(haveOne);